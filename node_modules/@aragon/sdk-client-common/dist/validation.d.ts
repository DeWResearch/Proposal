/**
 * Attempts to parse the given string as a URL and returns the IPFS CiD contained in it.
 * Alternatively it tries to use the raw value after validating it.
 *
 * @export
 * @param {string} data
 * @return {string}
 */
export declare function resolveIpfsCid(data: string): string;
/**
 * Checks if the given string is a valid proposal ID
 *
 * @export
 * @param {string} proposalId
 * @return {boolean}
 */
export declare function isProposalId(proposalId: string): boolean;
/**
 * Checks if the given string is a valid ENS name
 *
 * @export
 * @param {string} name
 * @return {boolean}
 */
export declare function isEnsName(name: string): boolean;
/**
 * Checks if the given string is a valid IPFS URI
 *
 * @export
 * @param {string} cid
 * @return {boolean}
 */
export declare function isIpfsUri(cid: string): boolean;
/**
 * Checks if the given string is a valid subdomain
 *
 * @export
 * @param {string} name
 * @return {boolean}
 */
export declare function isSubdomain(name: string): boolean;
