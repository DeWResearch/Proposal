import { IClient, IClientDecoding, IClientEncoding, IClientEstimation, IClientMethods } from "./internal/interfaces";
import { ClientCore, Context } from "@aragon/sdk-client-common";
/**
 * Provider a generic client with high level methods to manage and interact with DAO's
 */
export declare class Client extends ClientCore implements IClient {
    methods: IClientMethods;
    encoding: IClientEncoding;
    decoding: IClientDecoding;
    estimation: IClientEstimation;
    constructor(context: Context);
}
