import { CreateDaoParams, DepositParams, SetAllowanceParams } from "../../types";
import { IClientEstimation } from "../interfaces";
import { ClientCore, GasFeeEstimation, PrepareInstallationParams, PrepareUpdateParams } from "@aragon/sdk-client-common";
/**
 * Estimation module the SDK Generic Client
 */
export declare class ClientEstimation extends ClientCore implements IClientEstimation {
    prepareInstallation(params: PrepareInstallationParams): Promise<GasFeeEstimation>;
    /**
     * Estimates the gas fee of creating a DAO
     *
     * @param {CreateDaoParams} _params
     * @return {Promise<GasFeeEstimation>}
     * @memberof ClientEstimation
     */
    createDao(params: CreateDaoParams): Promise<GasFeeEstimation>;
    /**
     * Estimates the gas fee of depositing ether or an ERC20 token into the DAO
     * This does not estimate the gas cost of updating the allowance of an ERC20 token
     *
     * @param {DepositParams} params
     * @return {Promise<GasFeeEstimation>}
     * @memberof ClientEstimation
     */
    deposit(params: DepositParams): Promise<GasFeeEstimation>;
    /**
     * Estimates the gas fee of updating the allowance of an ERC20 token
     *
     * @param {SetAllowanceParams} _params
     * @return {Promise<GasFeeEstimation>}
     * @memberof ClientEstimation
     */
    setAllowance(params: SetAllowanceParams): Promise<GasFeeEstimation>;
    /**
     * Estimates the gas fee of preparing an update
     *
     * @param {PrepareUpdateParams} params
     * @return {Promise<GasFeeEstimation>}
     * @memberof ClientEstimation
     */
    prepareUpdate(params: PrepareUpdateParams): Promise<GasFeeEstimation>;
}
